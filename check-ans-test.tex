% arara: pdflatex
% arara: pdflatex
% arara: clean: { extensions: [ aux, log, out] }
\documentclass{article}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
%\usepackage{tikzducks}
\usepackage[colorlinks]{hyperref}
\usepackage{enumext}
%\pagestyle{empty}
\begin{document}

Este entorno tiene save-ans

\begin{enumext}[nosep,itemindent=0cm,save-ans={test-1990sssssss}, check-ans=false,]
  \item Related to Linux \anskey{Yes}%%, \anskey{Yes}%%\label{A}
  \item You use linux? \anskey{Yes} %%\anskey{Yes}
  \item Related to Linux \anskey{Yes}
  \item Related to Linux
    \begin{enumext}[no-store,label=\Roman*.,nosep]
    \item $2\alpha+2\delta=90^{\circ}$
    \item $\alpha=\delta$
    \item $\angle EDF=45^{\circ}$
  \end{enumext}
  \begin{keyans}[itemindent=0.5cm,columns=2,columns-sep=1cm]
  \item Choice \footnote{Hola}
  \item Choice
  \item Choice
  \item*[XY]Correct
  \end{keyans}
  \item Related to Linux
  \begin{keyans}[list-indent=-2cm, itemindent=1cm,columns=3, ref={HOLA-\arabic*.}]
  \item Choice\footnote{JAMAS}
  \item* Choice \label{A}
  \item Choice\footnote{CHAO}
  \end{keyans}
    \item Related to Linux
  \begin{keyans}[itemindent=0.5cm,columns=2,columns-sep=1cm]
  \item Choice \footnote{Queee}
  \item Choice
  \item Choice
  \item*[XY]Correct
  \end{keyans}
\end{enumext}
\ref{A}
\stop
\newpage

\begin{enumext*}[nosep,itemindent=0cm,save-ans={test-ssssssss}, check-ans=true,]

  \item Related to Linux \anskey{Yes}%%\label{A}
  \item You use linux? \anskey{Yes}
  \item Related to Linux \anskey{Yes}
  \item Related to Linux
  \begin{keyans}[itemindent=0.5cm,columns=2,columns-sep=1cm]
  \item Choice \footnote{Hola}
  \item Choice
  \item Choice
  \item*[XY]Correct
  \end{keyans}
  \item Related to Linux
  \begin{keyans}[list-indent=-2cm, itemindent=1cm,columns=3, ref={HOLA-\arabic*.}]
  \item Choice\footnote{JAMAS}
  \item* Choice %\label{A}
  \item Choice\footnote{CHAO}
  \end{keyans}
    \item Related to Linux
  \begin{keyans}[itemindent=0.5cm,columns=2,columns-sep=1cm]
  \item Choice \footnote{Queee}
  \item Choice
  \item Choice
  \item*[XY]Correct
  \end{keyans}
\end{enumext*}

Este entorno tiene \verb+series=A+
\begin{enumext}[label=\Roman*, nosep, series=A, save-ref=true, show-pos=true]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
\end{enumext}

\stop
Este entorno tiene \verb+resume*+
\begin{enumext}[resume*]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
\end{enumext}


Este entorno tiene \verb+resume*,,start=20+
\begin{enumext}[resume*,start=20]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
\end{enumext}

Este entorno tiene \verb+save-ans=test,resume=A+

\begin{enumext}[save-ans=test,resume=A]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
\end{enumext}

Este entorno tiene \verb+resume*+

\begin{enumext}[resume*]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
\end{enumext}


Este entorno tiene \verb+resume+

\begin{enumext}[resume]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
\end{enumext}

Este entorno tiene \verb+resume*+

\begin{enumext}[resume*]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
\end{enumext}

Este entorno tiene \verb+save-ans=test,resume=A+

\begin{enumext}[save-ans=test,resume=A]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
\end{enumext}

Este entorno tiene \verb+resume*+

\begin{enumext}[resume*]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
\end{enumext}

\stop

\begin{enumext}[series=B]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
\end{enumext}

\begin{enumext}[resume=A]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
\end{enumext}
\stop
C

\begin{enumext}[resume=A]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
\end{enumext}

D

\begin{enumext}[label={\roman*.)}]
  \item Related to Linux
  \item Related to Linux
  \item Related to Linux
\end{enumext}

E

\begin{enumext}[resume=A]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
\end{enumext}
\begin{enumext}[resume=A]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
\end{enumext}
\begin{enumext}[resume=A]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
\end{enumext}


\begin{enumext}[resume]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
\end{enumext}


\stop


Esta serie va del "a" al "f" guardado en \verb+series=A+

\begin{enumext}[label=\alph*, series=A]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
\end{enumext}

Aqui usamos \verb+label=(\arabic*.)+.

\begin{enumext}[label=(\arabic*.),]
  \item Uno
  \item Dos
  \item Tres
\end{enumext}

Y aquí? estamos pasando \verb+resume=A+ ???

\begin{enumext}[resume=A]
  \item Related to Linux
  \item Related to Linux
  \item Related to Linux
  \item Related to Linux
\end{enumext}

Aqui usamos \verb+label=\roman*, series=B+.

\begin{enumext}[label=\roman*, series=B]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
\end{enumext}

Aqui usamos \verb+resume=A+.

\begin{enumext}[resume=A]
  \item Related to Linux
  \item Related to Linux
  \item Related to Linux
  \item Related to Linux
\end{enumext}

Aqui usamos \verb+resume*+.

\begin{enumext}[resume]
  \item Related to Linux
  \item Related to Linux
  \item Related to Linux
  \item Related to Linux
\end{enumext}

\stop


Aqui estamos pasando \verb+resume*+, OK

\begin{enumext}[resume]
  \item Related to Linux
  \item Related to Linux
  \item Related to Linux
  \item Related to Linux
\end{enumext}
\stop



Aqui estamos pasando \verb+resume*+, OK

\begin{enumext}[resume*]
  \item Related to Linux
  \item Related to Linux
  \item Related to Linux
  \item Related to Linux
\end{enumext}
\stop



\begin{enumext}[save-ans=simplews,columns=1,check-ans=true,show-pos=true,show-pos=true,show-pos=true,nosep,save-ref=true,wrap-label={\fbox{#1}}]
  \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
  \item Related to Linux
    \begin{enumext}[nosep,columns*=2]
      \item You use linux? \anskey{Yes}
      \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
      \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
    \end{enumext}
   \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
   \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
   \item ALgo
         \begin{enumext}[nosep]
          \item* \texttt{xsim-exam} \anskey{doesn't exist for now :(}
           \item* \texttt{xsim-exam} \anskey{doesn't exist for now :(}
            \item* \texttt{xsim-exam} \anskey{doesn't exist for now :(}
        \end{enumext}
\end{enumext}

\section{CONTINUE}

%\printkeyans*[columns=2]{simplews}


\begin{enumext}[columns=1, resume, start=20, nosep,save-ref=true,wrap-label={\fbox{#1}}]
  \item Factor $x^{2}-2x+1$ %\anskey{$\left(x-1\right)^{2}$}
  \item Related to Linux
    \begin{enumext*}[nosep,columns*=2,start=M]
      \item You use linux? %\anskey{Yes}
      \item Factor $x^{2}-2x+1$ %\anskey{$\left(x-1\right)^{2}$}
      \item Factor $x^{2}-2x+1$ %\anskey{$\left(x-1\right)^{2}$}
    \end{enumext*}
   \item Factor $x^{2}-2x+1$ %\anskey{$\left(x-1\right)^{2}$}
   \item Factor $x^{2}-2x+1$ %\anskey{$\left(x-1\right)^{2}$}
   \item ALgo
         \begin{enumext}[nosep]
          \item* \texttt{xsim-exam} %\anskey{doesn't exist for now :(}
           \item* \texttt{xsim-exam} %\anskey{doesn't exist for now :(}
            \item* \texttt{xsim-exam} %\anskey{doesn't exist for now :(}
        \end{enumext}
\end{enumext}



\begin{enumext*}[columns=1]
  \item Related to Linux
    \begin{enumext}[nosep,columns*=2,start=M]
      \item You use linux? %\anskey{Yes}
    \end{enumext}
\end{enumext*}


\stop
\newpage


\begin{enumext}[save-ans=newww,columns=1,check-ans=true,show-pos=true,nosep,save-ref=true]
  \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
  \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
  \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
  \item Rate the following package and class
        \begin{enumext}[no-store,nosep]
          \item \texttt{xsim-exam} %\anskey{doesn't exist for now :(}
        \end{enumext}
        \anskey{WTF}
    \item Related to Linux
    \begin{enumext}[nosep,columns*=2]
      \item You use linux? \anskey{Yes}
      \item Rate the following package and class
        \begin{enumext*}[no-store,nosep]
          \item \texttt{xsim-exam} \anskey{NOT STORED ANS}
        \end{enumext*}
        \anskey{kkk}
    \end{enumext}
\end{enumext}


\stop


Before

\begin{enumext}[nosep,align=center,save-ans=mytest,show-ans=true,check-ans=true,save-ref=true,widest=99]
\item* First type of questions % 1
has a second optional argument
which allows selecting a range of sentences from the paragraphs.
%To get the sentences four to eight from paragraphs three to nine
%\anskey{JJJ}

\begin{keyans*}[itemindent=0.5cm,columns=2,columns-sep=1cm]
  \item Choice
  \item Choice
  \item Choice
  \item*[XY]Correct
\end{keyans*}

\item* First type of questions % 1
has a second optional argument
which allows selecting a range of sentences from the paragraphs.

\begin{keyans}[itemindent=0.5cm,columns=2,columns-sep=1cm]
  \item Choice
  \item Choice
  \columnbreak
  \item Choice
  \item*[XY] Correct
\end{keyans}


\item THIS iS KEYANS* ---which allows selecting a range of sentences from the paragraphs.

\begin{keyans*}[columns=2,columns-sep=1cm,show-ans=false]
  \item Starred has a second optional argument
which allows selecting a range of sentences from the paragraphs.
  \item Starred has a second optional argument
which allows selecting a range of sentences from the paragraphs.
  \item Starred has a second optional argument
which allows selecting a range of sentences from the paragraphs.
  \item*[X] Correct has a second optional argument
which allows selecting a range of sentences from the paragraphs.
\end{keyans*}

\item THIS IS KEYANS STANDAR
which allows selecting a range of sentences from the paragraphs.

\begin{keyans}[columns=2,columns-sep=1cm,]
  \item value
  \item*[Z] Correct has a second optional argument
which allows selecting a range of sentences from the paragraphs.
  \item value
  \item Starred has a second optional argument
which allows selecting a range of sentences from the paragraphs.
\end{keyans}


\item Third optional argument
which allows selecting a range of sentences from the paragraphs.

\begin{keyans*}[itemindent=0.5cm,columns=1]
  \item*[L] Correct has a second optional argument
which allows selecting a range of sentences from the paragraphs.


  \item[123456] Starred has a second optional argument
which allows selecting a range of sentences from the paragraphs.
  \item Starred has a second optional argument
which allows selecting a range of sentences from the paragraphs.
\end{keyans*}

\item Third optional argument
which allows selecting a range of sentences from the paragraphs.

\begin{keyans*}[itemindent=0.5cm,columns=2]
  \item*[K] Correct
  \item value
  \item value
  \item value
\end{keyans*}

\end{enumext}

%\ref{mytest:1} and \ref{mytest:2} and
Ref to \verb+\getkeyans{mytest:1}+ here:\getkeyans{mytest:1}\par
Ref to \verb+\ref{mytest:1}+ here:\ref{mytest:1}\par
Ref to \verb+\getkeyans{mytest:3}+ here:\getkeyans{mytest:3}\par
Ref to \verb+\ref{mytest:3}+ here:\ref{mytest:3}

\printkeyans[columns=1]{mytest}

\newpage
\stop





\begin{enumext}[save-ans=simplews,columns=1,check-ans=true,show-pos,nosep,save-ref]
  \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
  \item Related to Linux
    \begin{enumext}[nosep,columns*=2]
      \item You use linux? \anskey{Yes}
      \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
      \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
    \end{enumext}
   \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
   \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
   \item ALgo
      \begin{enumext*}[nosep]
         \item* \texttt{xsim-exam} \anskey{doesn't exist for now :(}
         \item* \texttt{xsim-exam} \anskey{doesn't exist for now :(}
         \item* \texttt{xsim-exam} \anskey{doesn't exist for now :(}
      \end{enumext*}
   \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
   \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
\end{enumext}

\stop

\begin{enumext}[save-ans=simplews-2,columns=2,check-ans=true,show-pos,nosep,save-ref]
  \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
  \item Related to Linux
    \begin{enumext}[nosep,columns*=2]
      \item You use linux? \anskey{Yes}
      \item Rate the following package and class
        \begin{enumext}[no-store,nosep]
          \item \texttt{xsim-exam} \anskey{doesn't exist for now :(}
        \end{enumext}
        \anskey{kkk}
    \end{enumext}
       \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
   \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
     \item ALgo
         \begin{enumext*}[nosep]
          \item* \texttt{xsim-exam} \anskey{doesn't exist for now :(}
           \item* \texttt{xsim-exam} \anskey{doesn't exist for now :(}
            \item* \texttt{xsim-exam} \anskey{doesn't exist for now :(}
        \end{enumext*}
\end{enumext}

\begin{enumext}[save-ans=simplews,columns=1,check-ans=true,show-pos,nosep,save-ref]
  \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
  \item Related to Linux
    \begin{enumext}[nosep,columns*=2]
      \item You use linux? \anskey{Yes}
      \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
      \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
    \end{enumext}
   \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
   \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
   \item ALgo
         \begin{enumext}[nosep]
          \item* \texttt{xsim-exam} \anskey{doesn't exist for now :(}
           \item* \texttt{xsim-exam} \anskey{doesn't exist for now :(}
            \item* \texttt{xsim-exam} \anskey{doesn't exist for now :(}
        \end{enumext}
\end{enumext}



\begin{enumext}[save-ans=simplews-2,columns=2,check-ans=true,show-pos,nosep,save-ref]
  \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
  \item Related to Linux
    \begin{enumext}[nosep,columns*=2]
      \item You use linux? \anskey{Yes}
      \item Rate the following package and class
        \begin{enumext}[no-store,nosep]
          \item \texttt{xsim-exam} \anskey{doesn't exist for now :(}
        \end{enumext}
        \anskey{kkk}
    \end{enumext}
\end{enumext}
\stop














\begin{enumext*}[save-ans=simplews,columns=2,check-ans=true,show-pos,nosep,save-ref]
  \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
  \item Related to Linux
    \begin{enumext}[nosep,columns*=2]
      \item You use linux? \anskey{Yes}
      \item Rate the following package and class
        \begin{enumext}[nosep]
          \item \texttt{xsim-exam} \anskey{doesn't exist for now :(}
        \end{enumext}
    \end{enumext}
\end{enumext*}

The answer to \ref{simplews:1} is \getkeyans{simplews:1} and the answer
to \ref{simplews:3} is \getkeyans{simplews:3}.

\printkeyans[columns=1]{simplews}












%%\end{document}
\newpage

\section{KEYANS}

    %\begin{enumext*}[itemindent=0.5cm,columns=2,show-length]
        %\item value
        %\item* correct \footnote{CUAK}
    %\end{enumext*}


Before

\begin{enumext}[nosep,align=center,save-ans=mytest,show-ans,check-ans=true,save-ref,widest=99,]
\item* First type of questions % 1
has a second optional argument
which allows selecting a range of sentences from the paragraphs.
%To get the sentences four to eight from paragraphs three to nine
%\anskey{JJJ}

\begin{keyans}[itemindent=1.5cm,columns=2,columns-sep=1cm]
  \item[123456] Starred has a second optional argument
which allows selecting a range of sentences from the paragraphs.
  \item Starred has a second optional argument
which allows selecting a range of sentences from the paragraphs.
  \item Starred has a second optional argument
which allows selecting a range of sentences from the paragraphs.
  \item*[X] Correct has a second optional argument
which allows selecting a range of sentences from the paragraphs.
\end{keyans}

\item second optional argument
which allows selecting a range of sentences from the paragraphs.

\begin{keyans}[itemindent=0.5cm,columns=2]
  \item[123456789ABC] NO Starred
  \item value
  \item*[Z]  Starred has a second optional argument
which allows selecting a range of sentences from the paragraphs.
  \item value
  \item Starred has a second optional argument
which allows selecting a range of sentences from the paragraphs.
\end{keyans}


\item Third optional argument
which allows selecting a range of sentences from the paragraphs.

\begin{keyans}[itemindent=0.5cm,columns=1]
  \item*[L] Correct has a second optional argument
which allows selecting a range of sentences from the paragraphs.


  \item[123456] Starred has a second optional argument
which allows selecting a range of sentences from the paragraphs.
  \item Starred has a second optional argument
which allows selecting a range of sentences from the paragraphs.
\end{keyans}

\item Third optional argument
which allows selecting a range of sentences from the paragraphs.

\begin{keyans}[itemindent=0.5cm,columns=2]
  \item*[K] Correct
  \item value
  \item value
  \item value
\end{keyans}

\end{enumext}

%\ref{mytest:1} and \ref{mytest:2} and
Ref to \verb+\getkeyans{mytest:1}+ here:\getkeyans{mytest:1}\par
Ref to \verb+\ref{mytest:1}+ here:\ref{mytest:1}\par
Ref to \verb+\getkeyans{mytest:3}+ here:\getkeyans{mytest:3}\par
Ref to \verb+\ref{mytest:3}+ here:\ref{mytest:3}

\printkeyans[columns=1]{mytest}

\newpage

\begin{enumext*}[save-ans=simplews,columns=2,check-ans=true,show-pos,nosep,save-ref]
  \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
  \item Related to Linux
    \begin{enumext}[nosep,columns*=2]
      \item You use linux? \anskey{Yes}
      \item Rate the following package and class
        \begin{enumext}[nosep]
          \item \texttt{xsim-exam} \anskey{doesn't exist for now :(}
        \end{enumext}
    \end{enumext}
\end{enumext*}
\end{document}





\item* First type of questions % 1
has a second optional argument
which allows selecting a range of sentences from the paragraphs.
To get the sentences four to eight from paragraphs three to nine
    %\begin{keyans*}[itemindent=0.5cm,columns=2,show-length]
        %\item(2) value \footnote{HOLA}
        %\item ZZZZZZZZZZZZZZZZZZZZz
        %\item(2)*[SOME] correct To get the sentences four to eight from paragraphs three to nine
        %\item value

    %\end{keyans*}

\item* First type of questions % 1
has a second optional argument
which allows selecting a range of sentences from the paragraphs.
To get the sentences four to eight from paragraphs three to nine

   %\begin{keyans*}[itemindent=0.5cm,columns=2,show-length]
        %\item(2)*[SOME] correct To get the sentences four to eight from paragraphs three to nine
        %\item(2) value \footnote{HOLA}
        %\item ZZZZZZZZZZZZZZZZZZZZz

        %\item value

    %\end{keyans*}
\end{enumext}

\ref{mytest:1} and \ref{mytest:2} and \getkeyans{mytest:2}

\printkeyans[columns=2]{mytest}
\end{document}

















\newpage

\begin{enumext}[save-ans=simplews,columns=2,check-ans=true,show-ans,nosep,save-ref,wrap-label={\tikz[scale=0.25]\duck[signpost=\scalebox{0.6}{#1}];}]
  \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
  \item Factor $3x+3y+3z$ \anskey{$3(x+y+z)$}
  \item True False
    \begin{enumext}[nosep]
      \item $\alpha > \delta$ \anskey{False}
      \item \LaTeX2e\ is cool? \anskey{Very True!}
    \end{enumext}
  \item Related to Linux
    \begin{enumext}[nosep]
      \item You use linux? \anskey{Yes}
      \item Usually uses the package manager? \anskey{Yes, \texttt{dnf}}
      \item Rate the following package and class
        \begin{enumext}[nosep]
          \item \texttt{xsim-exam} \anskey{doesn't exist for now :(}
          \item \texttt{xsim} \anskey{very good}
          \item \texttt{exsheets} \anskey{obsolete}
        \end{enumext}
    \end{enumext}
\end{enumext}

The answer to \ref{simplews:1} is \getkeyans{simplews:1} and the answer
to \ref{simplews:3} is \getkeyans{simplews:3}.

\printkeyans[columns=2]{simplews}

%%\end{document}

\section{KEYANS}

Before
\begin{enumext}[nosep,itemsep=0pt,align=center,save-ans=mytest,check-ans=true,columns=1,save-ref,widest=99,]
\item* First type of questions % 1
     XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXx
     XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX


    \begin{keyans*}[itemindent=0.5cm,columns=2,show-length]
        \item value
        \item* correct
    \end{keyans*}

\item* First type of questions % 1
     XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXx
     XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX


    \begin{keyans*}[itemindent=0.5cm,columns=2,show-length]
        \item value
        \item* correct
    \end{keyans*}
\end{enumext}

\end{document}%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

