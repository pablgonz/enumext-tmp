% arara: pdflatex
% arara: pdflatex
% arara: clean: { extensions: [ aux, log, out] }
\documentclass{article}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
%\usepackage{tikzducks}
\usepackage[colorlinks]{hyperref}
\usepackage{enumitem}
%\pagestyle{empty}
\begin{document}

Este entorno tiene save-ans
\begin{enumerate}[nosep,itemindent=2cm, labelsep=4cm]
  \item[Z] Related to Linux Related to Linux Related to Linux Related to Linux
  Related to Linux Related to Linux Related to Linux Related to Linux

  Related to Linux Related to Linux Related to Linux Related to Linux
  Related to Linux Related to Linux Related to Linux Related to Linux

  \item Related to Linux Related to Linux Related to Linux Related to Linux
  Related to Linux Related to Linux Related to Linux Related to Linux

  Related to Linux Related to Linux Related to Linux Related to Linux
  Related to Linux Related to Linux Related to Linux Related to Linux
  \item Related to Linux
\end{enumerate}

Este entorno tiene \verb+series=A+
\begin{enumerate}[label=\Roman*, nosep, series=A]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
\end{enumerate}

Este entorno tiene \verb+resume*+
\begin{enumerate}[resume*]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
\end{enumerate}


Este entorno tiene \verb+resume*,,start=20+
\begin{enumerate}[resume*,start=20]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
\end{enumerate}

Este entorno tiene \verb+save-ans=test,resume=A+

\begin{enumerate}[resume=A]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
\end{enumerate}

Este entorno tiene \verb+resume*+

\begin{enumerate}[resume*]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
\end{enumerate}


Este entorno tiene \verb+resume+

\begin{enumerate}[resume]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
\end{enumerate}

Este entorno tiene \verb+resume*+

\begin{enumerate}[resume*]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
\end{enumerate}

Este entorno tiene \verb+save-ans=test,resume=A+

\begin{enumerate}[resume=A]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
\end{enumerate}

Este entorno tiene \verb+resume*+

\begin{enumerate}[resume*]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
\end{enumerate}

\stop

\begin{enumerate}[series=B]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
\end{enumerate}

\begin{enumerate}[resume=A]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
\end{enumerate}
\stop
C

\begin{enumerate}[resume=A]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
\end{enumerate}

D

\begin{enumerate}[label={\roman*.)}]
  \item Related to Linux
  \item Related to Linux
  \item Related to Linux
\end{enumerate}

E

\begin{enumerate}[resume=A]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
\end{enumerate}
\begin{enumerate}[resume=A]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
\end{enumerate}
\begin{enumerate}[resume=A]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
\end{enumerate}


\begin{enumerate}[resume]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
\end{enumerate}


\stop


Esta serie va del "a" al "f" guardado en \verb+series=A+

\begin{enumerate}[label=\alph*, series=A]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
\end{enumerate}

Aqui usamos \verb+label=(\arabic*.)+.

\begin{enumerate}[label=(\arabic*.),]
  \item Uno
  \item Dos
  \item Tres
\end{enumerate}

Y aquí? estamos pasando \verb+resume=A+ ???

\begin{enumerate}[resume=A]
  \item Related to Linux
  \item Related to Linux
  \item Related to Linux
  \item Related to Linux
\end{enumerate}

Aqui usamos \verb+label=\roman*, series=B+.

\begin{enumerate}[label=\roman*, series=B]
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
  \item Related to Linux
  \item You use linux? %\anskey{Yes}
\end{enumerate}

Aqui usamos \verb+resume=A+.

\begin{enumerate}[resume=A]
  \item Related to Linux
  \item Related to Linux
  \item Related to Linux
  \item Related to Linux
\end{enumerate}

Aqui usamos \verb+resume*+.

\begin{enumerate}[resume]
  \item Related to Linux
  \item Related to Linux
  \item Related to Linux
  \item Related to Linux
\end{enumerate}

\stop


Aqui estamos pasando \verb+resume*+, OK

\begin{enumerate}[resume]
  \item Related to Linux
  \item Related to Linux
  \item Related to Linux
  \item Related to Linux
\end{enumerate}
\stop



Aqui estamos pasando \verb+resume*+, OK

\begin{enumerate}[resume*]
  \item Related to Linux
  \item Related to Linux
  \item Related to Linux
  \item Related to Linux
\end{enumerate}
\stop



\begin{enumerate}[save-ans=simplews,columns=1,check-ans=true,show-pos=true,show-pos=true,show-pos=true,nosep,save-ref=true,wrap-label={\fbox{#1}}]
  \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
  \item Related to Linux
    \begin{enumerate}[nosep,columns*=2]
      \item You use linux? \anskey{Yes}
      \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
      \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
    \end{enumerate}
   \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
   \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
   \item ALgo
         \begin{enumerate}[nosep]
          \item* \texttt{xsim-exam} \anskey{doesn't exist for now :(}
           \item* \texttt{xsim-exam} \anskey{doesn't exist for now :(}
            \item* \texttt{xsim-exam} \anskey{doesn't exist for now :(}
        \end{enumerate}
\end{enumerate}

\section{CONTINUE}

%\printkeyans*[columns=2]{simplews}


\begin{enumerate}[columns=1, resume, start=20, nosep,save-ref=true,wrap-label={\fbox{#1}}]
  \item Factor $x^{2}-2x+1$ %\anskey{$\left(x-1\right)^{2}$}
  \item Related to Linux
    \begin{enumerate*}[nosep,columns*=2,start=M]
      \item You use linux? %\anskey{Yes}
      \item Factor $x^{2}-2x+1$ %\anskey{$\left(x-1\right)^{2}$}
      \item Factor $x^{2}-2x+1$ %\anskey{$\left(x-1\right)^{2}$}
    \end{enumerate*}
   \item Factor $x^{2}-2x+1$ %\anskey{$\left(x-1\right)^{2}$}
   \item Factor $x^{2}-2x+1$ %\anskey{$\left(x-1\right)^{2}$}
   \item ALgo
         \begin{enumerate}[nosep]
          \item* \texttt{xsim-exam} %\anskey{doesn't exist for now :(}
           \item* \texttt{xsim-exam} %\anskey{doesn't exist for now :(}
            \item* \texttt{xsim-exam} %\anskey{doesn't exist for now :(}
        \end{enumerate}
\end{enumerate}



\begin{enumerate*}[columns=1]
  \item Related to Linux
    \begin{enumerate}[nosep,columns*=2,start=M]
      \item You use linux? %\anskey{Yes}
    \end{enumerate}
\end{enumerate*}


\stop
\newpage


\begin{enumerate}[save-ans=newww,columns=1,check-ans=true,show-pos=true,nosep,save-ref=true]
  \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
  \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
  \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
  \item Rate the following package and class
        \begin{enumerate}[no-store,nosep]
          \item \texttt{xsim-exam} %\anskey{doesn't exist for now :(}
        \end{enumerate}
        \anskey{WTF}
    \item Related to Linux
    \begin{enumerate}[nosep,columns*=2]
      \item You use linux? \anskey{Yes}
      \item Rate the following package and class
        \begin{enumerate*}[no-store,nosep]
          \item \texttt{xsim-exam} \anskey{NOT STORED ANS}
        \end{enumerate*}
        \anskey{kkk}
    \end{enumerate}
\end{enumerate}


\stop


Before

\begin{enumerate}[nosep,align=center,save-ans=mytest,show-ans=true,check-ans=true,save-ref=true,widest=99]
\item* First type of questions % 1
has a second optional argument
which allows selecting a range of sentences from the paragraphs.
%To get the sentences four to eight from paragraphs three to nine
%\anskey{JJJ}

\begin{keyans*}[itemindent=0.5cm,columns=2,columns-sep=1cm]
  \item Choice
  \item Choice
  \item Choice
  \item*[XY]Correct
\end{keyans*}

\item* First type of questions % 1
has a second optional argument
which allows selecting a range of sentences from the paragraphs.

\begin{keyans}[itemindent=0.5cm,columns=2,columns-sep=1cm]
  \item Choice
  \item Choice
  \columnbreak
  \item Choice
  \item*[XY] Correct
\end{keyans}


\item THIS iS KEYANS* ---which allows selecting a range of sentences from the paragraphs.

\begin{keyans*}[columns=2,columns-sep=1cm,show-ans=false]
  \item Starred has a second optional argument
which allows selecting a range of sentences from the paragraphs.
  \item Starred has a second optional argument
which allows selecting a range of sentences from the paragraphs.
  \item Starred has a second optional argument
which allows selecting a range of sentences from the paragraphs.
  \item*[X] Correct has a second optional argument
which allows selecting a range of sentences from the paragraphs.
\end{keyans*}

\item THIS IS KEYANS STANDAR
which allows selecting a range of sentences from the paragraphs.

\begin{keyans}[columns=2,columns-sep=1cm,]
  \item value
  \item*[Z] Correct has a second optional argument
which allows selecting a range of sentences from the paragraphs.
  \item value
  \item Starred has a second optional argument
which allows selecting a range of sentences from the paragraphs.
\end{keyans}


\item Third optional argument
which allows selecting a range of sentences from the paragraphs.

\begin{keyans*}[itemindent=0.5cm,columns=1]
  \item*[L] Correct has a second optional argument
which allows selecting a range of sentences from the paragraphs.


  \item[123456] Starred has a second optional argument
which allows selecting a range of sentences from the paragraphs.
  \item Starred has a second optional argument
which allows selecting a range of sentences from the paragraphs.
\end{keyans*}

\item Third optional argument
which allows selecting a range of sentences from the paragraphs.

\begin{keyans*}[itemindent=0.5cm,columns=2]
  \item*[K] Correct
  \item value
  \item value
  \item value
\end{keyans*}

\end{enumerate}

%\ref{mytest:1} and \ref{mytest:2} and
Ref to \verb+\getkeyans{mytest:1}+ here:\getkeyans{mytest:1}\par
Ref to \verb+\ref{mytest:1}+ here:\ref{mytest:1}\par
Ref to \verb+\getkeyans{mytest:3}+ here:\getkeyans{mytest:3}\par
Ref to \verb+\ref{mytest:3}+ here:\ref{mytest:3}

\printkeyans[columns=1]{mytest}

\newpage
\stop





\begin{enumerate}[save-ans=simplews,columns=1,check-ans=true,show-pos,nosep,save-ref]
  \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
  \item Related to Linux
    \begin{enumerate}[nosep,columns*=2]
      \item You use linux? \anskey{Yes}
      \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
      \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
    \end{enumerate}
   \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
   \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
   \item ALgo
      \begin{enumerate*}[nosep]
         \item* \texttt{xsim-exam} \anskey{doesn't exist for now :(}
         \item* \texttt{xsim-exam} \anskey{doesn't exist for now :(}
         \item* \texttt{xsim-exam} \anskey{doesn't exist for now :(}
      \end{enumerate*}
   \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
   \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
\end{enumerate}

\stop

\begin{enumerate}[save-ans=simplews-2,columns=2,check-ans=true,show-pos,nosep,save-ref]
  \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
  \item Related to Linux
    \begin{enumerate}[nosep,columns*=2]
      \item You use linux? \anskey{Yes}
      \item Rate the following package and class
        \begin{enumerate}[no-store,nosep]
          \item \texttt{xsim-exam} \anskey{doesn't exist for now :(}
        \end{enumerate}
        \anskey{kkk}
    \end{enumerate}
       \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
   \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
     \item ALgo
         \begin{enumerate*}[nosep]
          \item* \texttt{xsim-exam} \anskey{doesn't exist for now :(}
           \item* \texttt{xsim-exam} \anskey{doesn't exist for now :(}
            \item* \texttt{xsim-exam} \anskey{doesn't exist for now :(}
        \end{enumerate*}
\end{enumerate}

\begin{enumerate}[save-ans=simplews,columns=1,check-ans=true,show-pos,nosep,save-ref]
  \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
  \item Related to Linux
    \begin{enumerate}[nosep,columns*=2]
      \item You use linux? \anskey{Yes}
      \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
      \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
    \end{enumerate}
   \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
   \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
   \item ALgo
         \begin{enumerate}[nosep]
          \item* \texttt{xsim-exam} \anskey{doesn't exist for now :(}
           \item* \texttt{xsim-exam} \anskey{doesn't exist for now :(}
            \item* \texttt{xsim-exam} \anskey{doesn't exist for now :(}
        \end{enumerate}
\end{enumerate}



\begin{enumerate}[save-ans=simplews-2,columns=2,check-ans=true,show-pos,nosep,save-ref]
  \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
  \item Related to Linux
    \begin{enumerate}[nosep,columns*=2]
      \item You use linux? \anskey{Yes}
      \item Rate the following package and class
        \begin{enumerate}[no-store,nosep]
          \item \texttt{xsim-exam} \anskey{doesn't exist for now :(}
        \end{enumerate}
        \anskey{kkk}
    \end{enumerate}
\end{enumerate}
\stop














\begin{enumerate*}[save-ans=simplews,columns=2,check-ans=true,show-pos,nosep,save-ref]
  \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
  \item Related to Linux
    \begin{enumerate}[nosep,columns*=2]
      \item You use linux? \anskey{Yes}
      \item Rate the following package and class
        \begin{enumerate}[nosep]
          \item \texttt{xsim-exam} \anskey{doesn't exist for now :(}
        \end{enumerate}
    \end{enumerate}
\end{enumerate*}

The answer to \ref{simplews:1} is \getkeyans{simplews:1} and the answer
to \ref{simplews:3} is \getkeyans{simplews:3}.

\printkeyans[columns=1]{simplews}












%%\end{document}
\newpage

\section{KEYANS}

    %\begin{enumerate*}[itemindent=0.5cm,columns=2,show-length]
        %\item value
        %\item* correct \footnote{CUAK}
    %\end{enumerate*}


Before

\begin{enumerate}[nosep,align=center,save-ans=mytest,show-ans,check-ans=true,save-ref,widest=99,]
\item* First type of questions % 1
has a second optional argument
which allows selecting a range of sentences from the paragraphs.
%To get the sentences four to eight from paragraphs three to nine
%\anskey{JJJ}

\begin{keyans}[itemindent=1.5cm,columns=2,columns-sep=1cm]
  \item[123456] Starred has a second optional argument
which allows selecting a range of sentences from the paragraphs.
  \item Starred has a second optional argument
which allows selecting a range of sentences from the paragraphs.
  \item Starred has a second optional argument
which allows selecting a range of sentences from the paragraphs.
  \item*[X] Correct has a second optional argument
which allows selecting a range of sentences from the paragraphs.
\end{keyans}

\item second optional argument
which allows selecting a range of sentences from the paragraphs.

\begin{keyans}[itemindent=0.5cm,columns=2]
  \item[123456789ABC] NO Starred
  \item value
  \item*[Z]  Starred has a second optional argument
which allows selecting a range of sentences from the paragraphs.
  \item value
  \item Starred has a second optional argument
which allows selecting a range of sentences from the paragraphs.
\end{keyans}


\item Third optional argument
which allows selecting a range of sentences from the paragraphs.

\begin{keyans}[itemindent=0.5cm,columns=1]
  \item*[L] Correct has a second optional argument
which allows selecting a range of sentences from the paragraphs.


  \item[123456] Starred has a second optional argument
which allows selecting a range of sentences from the paragraphs.
  \item Starred has a second optional argument
which allows selecting a range of sentences from the paragraphs.
\end{keyans}

\item Third optional argument
which allows selecting a range of sentences from the paragraphs.

\begin{keyans}[itemindent=0.5cm,columns=2]
  \item*[K] Correct
  \item value
  \item value
  \item value
\end{keyans}

\end{enumerate}

%\ref{mytest:1} and \ref{mytest:2} and
Ref to \verb+\getkeyans{mytest:1}+ here:\getkeyans{mytest:1}\par
Ref to \verb+\ref{mytest:1}+ here:\ref{mytest:1}\par
Ref to \verb+\getkeyans{mytest:3}+ here:\getkeyans{mytest:3}\par
Ref to \verb+\ref{mytest:3}+ here:\ref{mytest:3}

\printkeyans[columns=1]{mytest}

\newpage

\begin{enumerate*}[save-ans=simplews,columns=2,check-ans=true,show-pos,nosep,save-ref]
  \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
  \item Related to Linux
    \begin{enumerate}[nosep,columns*=2]
      \item You use linux? \anskey{Yes}
      \item Rate the following package and class
        \begin{enumerate}[nosep]
          \item \texttt{xsim-exam} \anskey{doesn't exist for now :(}
        \end{enumerate}
    \end{enumerate}
\end{enumerate*}
\end{document}





\item* First type of questions % 1
has a second optional argument
which allows selecting a range of sentences from the paragraphs.
To get the sentences four to eight from paragraphs three to nine
    %\begin{keyans*}[itemindent=0.5cm,columns=2,show-length]
        %\item(2) value \footnote{HOLA}
        %\item ZZZZZZZZZZZZZZZZZZZZz
        %\item(2)*[SOME] correct To get the sentences four to eight from paragraphs three to nine
        %\item value

    %\end{keyans*}

\item* First type of questions % 1
has a second optional argument
which allows selecting a range of sentences from the paragraphs.
To get the sentences four to eight from paragraphs three to nine

   %\begin{keyans*}[itemindent=0.5cm,columns=2,show-length]
        %\item(2)*[SOME] correct To get the sentences four to eight from paragraphs three to nine
        %\item(2) value \footnote{HOLA}
        %\item ZZZZZZZZZZZZZZZZZZZZz

        %\item value

    %\end{keyans*}
\end{enumerate}

\ref{mytest:1} and \ref{mytest:2} and \getkeyans{mytest:2}

\printkeyans[columns=2]{mytest}
\end{document}

















\newpage

\begin{enumerate}[save-ans=simplews,columns=2,check-ans=true,show-ans,nosep,save-ref,wrap-label={\tikz[scale=0.25]\duck[signpost=\scalebox{0.6}{#1}];}]
  \item Factor $x^{2}-2x+1$ \anskey{$\left(x-1\right)^{2}$}
  \item Factor $3x+3y+3z$ \anskey{$3(x+y+z)$}
  \item True False
    \begin{enumerate}[nosep]
      \item $\alpha > \delta$ \anskey{False}
      \item \LaTeX2e\ is cool? \anskey{Very True!}
    \end{enumerate}
  \item Related to Linux
    \begin{enumerate}[nosep]
      \item You use linux? \anskey{Yes}
      \item Usually uses the package manager? \anskey{Yes, \texttt{dnf}}
      \item Rate the following package and class
        \begin{enumerate}[nosep]
          \item \texttt{xsim-exam} \anskey{doesn't exist for now :(}
          \item \texttt{xsim} \anskey{very good}
          \item \texttt{exsheets} \anskey{obsolete}
        \end{enumerate}
    \end{enumerate}
\end{enumerate}

The answer to \ref{simplews:1} is \getkeyans{simplews:1} and the answer
to \ref{simplews:3} is \getkeyans{simplews:3}.

\printkeyans[columns=2]{simplews}

%%\end{document}

\section{KEYANS}

Before
\begin{enumerate}[nosep,itemsep=0pt,align=center,save-ans=mytest,check-ans=true,columns=1,save-ref,widest=99,]
\item* First type of questions % 1
     XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXx
     XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX


    \begin{keyans*}[itemindent=0.5cm,columns=2,show-length]
        \item value
        \item* correct
    \end{keyans*}

\item* First type of questions % 1
     XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXx
     XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX


    \begin{keyans*}[itemindent=0.5cm,columns=2,show-length]
        \item value
        \item* correct
    \end{keyans*}
\end{enumerate}

\end{document}%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

